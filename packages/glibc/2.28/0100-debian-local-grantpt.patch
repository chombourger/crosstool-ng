---
 sysdeps/unix/grantpt.c |    6 +++++-
 1 file changed, 5 insertions(+), 1 deletion(-)

Index: glibc-2.28/sysdeps/unix/grantpt.c
===================================================================
--- glibc-2.28.orig/sysdeps/unix/grantpt.c
+++ glibc-2.28/sysdeps/unix/grantpt.c
@@ -219,9 +219,13 @@ grantpt (int fd)
   else
     {
       int w;
-
+retry:
       if (__waitpid (pid, &w, 0) == -1)
+	{
+	if (errno == EINTR)
+	    goto retry;	
 	goto cleanup;
+	}; 
       if (!WIFEXITED (w))
 	__set_errno (ENOEXEC);
       else
